module.exports = {
  // مدة استقبال الإجابات بعد فتح الشات (بالثواني)
  QUESTION_TIMEOUT: 30,

  // العد التنازلي قبل عرض كل سؤال (بالثواني)
  COUNTDOWN_SECONDS: 3,

  // مهلة القراءة بعد عرض السؤال وقبل فتح الشات (بالثواني)
  OPEN_DELAY_SECONDS: 3,

  // فاصل زمني بسيط بين رسائل البوت المتتالية (بالثواني) — عشان ما تطلع كلها دفعة وحدة
  MESSAGE_GAP_SECONDS: 3,

  // مدة الانتظار بعد رسالة شرح البطولة وقبل بداية الجولة الأولى (بالثواني) — ربع دقيقة لأنها لحظة مميزة
  TOURNAMENT_INTRO_WAIT_SECONDS: 15,
};

